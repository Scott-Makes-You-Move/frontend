apiVersion: v1
kind: Service
metadata:
  name: { { include "smym-frontend.fullname" . } }
  labels: { { - include "smym-frontend.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "smym-frontend.selectorLabels" . | nindent 4 } }
